
##############################################
########  OME C3P0 properties file   #########
##############################################


#### Logical properties #############################################

logical.jdbcUrl=jdbc:postgresql://vpdb1.aws.AL.com:5437/pgvprdev000
logical.username=Vuser
logical.password=1AL1

logical.minPoolSize=1

logical.maxPoolSize=5

# Seconds a Connection can remain pooled but unused before being discarded. Zero means idle connections never expire
logical.maxIdleTime=3600

# Seconds, effectively a time to live. A Connection older than maxConnectionAge will be destroyed and purged from the pool
logical.maxConnectionAge=21600

logical.acquireIncrement=3

logical.maxIdleTimeExcessConnections=0

logical.maxStatements=0

logical.maxStatementsPerConnection=0




##### Transactional Properties   #####################################################

trans.jdbcUrl=jdbc:edb://vpdb1.aws.AL.com:5437/pgvprdev001
trans.username=V_tx
trans.password=1AL1

trans.minPoolSize=1

trans.maxPoolSize=5

# Seconds a Connection can remain pooled but unused before being discarded. Zero means idle connections never expire
trans.maxIdleTime=3600

# Seconds, effectively a time to live. A Connection older than maxConnectionAge will be destroyed and purged from the pool
trans.maxConnectionAge=21600

trans.acquireIncrement=3

trans.maxIdleTimeExcessConnections=0

trans.maxStatements=0

trans.maxStatementsPerConnection=0
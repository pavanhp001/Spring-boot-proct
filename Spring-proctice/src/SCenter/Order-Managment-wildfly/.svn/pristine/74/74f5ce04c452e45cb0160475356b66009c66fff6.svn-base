package com.AL.ome.product;


public class TestProductManager {
	private static final String PRODUCT_SERVICE_URL = "http://sgdv3vm4:8080/product-01.00.00-SNAPSHOT-product/ProductWS";
	//private static JaxWsProxyFactoryBean factory;
	//private static ProductWS client;
	
	/**
	 * A method to send request to pricing via apache cxf client and return response. This class used to test pricing via OME in junit testcases.
	 * @param pricingRequest
	 * @return
	 */
	/*public static String sendToProduct(String productRequest){
		factory = new JaxWsProxyFactoryBean();
		factory.getInInterceptors().add(new LoggingInInterceptor());
		factory.getOutInterceptors().add(new LoggingOutInterceptor());
		factory.setServiceClass(ProductWS.class);
		factory.setAddress(PRODUCT_SERVICE_URL);
		client = (ProductWS) factory.create();
		String response = client.processRequest(productRequest);
		return response;
	}*/
}

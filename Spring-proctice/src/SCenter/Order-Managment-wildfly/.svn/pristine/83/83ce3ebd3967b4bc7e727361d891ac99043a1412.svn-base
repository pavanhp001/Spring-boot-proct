update V_tx.cm_consumer set firstname = UPPER(firstname), lastname = UPPER(lastname);

update V_tx.cm_address set street_name = UPPER(street_name), line2 = UPPER(line2), city = UPPER(city), state_or_prov = UPPER(state_or_prov);

commit;
package com.AL.ome.product;

import static org.junit.Assert.*;

import org.apache.xmlbeans.XmlException;
import org.junit.Before;
import org.junit.Test;

import com.AL.ome.system.BaseALTestX;
import com.AL.xml.v4.AcMessageDocument;
import com.AL.xml.v4.AcMessageType;
import com.AL.xml.v4.ProductEnterpriseResponseDocument;

public class ProductTest {
	
	private String productRequest;
	@Before
	public void setup(){
		productRequest = BaseALTestX.getXMLFromFile("src\\test\\resources\\xml\\productRequest.xml");
	}
	
	@Test
	public void testProduct() throws XmlException{
		/*assertNotNull(productRequest);
		String response = TestProductManager.sendToProduct(productRequest);
		assertNotNull(response);
		AcMessageDocument acMessage = AcMessageDocument.Factory.parse(response);
		assertNotNull(acMessage);
		assertNotNull(acMessage.getAcMessage().getPayload());
		AcMessageType.Payload pld = acMessage.getAcMessage().getPayload();
		ProductEnterpriseResponseDocument prodRes = ProductEnterpriseResponseDocument.Factory.parse(pld.toString());
		assertNotNull(prodRes);
		System.out.println("********Response************");
		System.out.println(response);
		System.out.println("********************");*/
	}
}

package com.A.rules.core;

import org.drools.runtime.StatefulKnowledgeSession;

/**
 * @author ebthomas
 * 
 */
public interface KnowledgeSessionLookup {
	/**
	 * .
	 * 
	 * @return creates a new session
	 */
	StatefulKnowledgeSession newSession();

	/**
	 * @param sessionId
	 *            session id to be loaded
	 * @return Stateful Knowledge session that will be created
	 */
	StatefulKnowledgeSession loadSession(final int sessionId);
}
